{
  "version": 1,
  "enabled": false,
  "id": "e2e",
  "default_profile": "local",
  "profiles": {
    "local": {
      "command": "",
      "env_file": "e2e/.env",
      "requires_env": ["E2E_BASE_URL"],
      "requires_files": ["e2e/.env"]
    },
    "ci": {
      "command": "",
      "env_file": null,
      "requires_env": ["E2E_BASE_URL"],
      "requires_files": []
    }
  },
  "notes": [
    "enabled=false by default; set true after configuring a working E2E command.",
    "Select a profile via `--e2e-profile <name>` (or defaults to default_profile).",
    "Keep secrets in e2e/.env (local) or CI environment variables (ci)."
  ]
}
