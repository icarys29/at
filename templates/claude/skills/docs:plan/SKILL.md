---
name: docs:plan
version: "0.5.0"
updated: "2026-02-02"
description: Show deterministic docs plan (no edits).
argument-hint: "[--session <id|dir>]"
allowed-tools: Read, Bash
---

# /docs:plan

Delegates to `docs-keeper` in `plan` mode (no repo edits).

- Task: `docs-keeper`

