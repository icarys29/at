# Architecture Records (ARDs)

Use ARDs when a component/framework needs a concise internal design record:

- Responsibilities
- Structure
- Flow
- Boundaries

Create new ARDs using the `ard` doc type template in `docs/_templates/`.

