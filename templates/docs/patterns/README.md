# Pattern Docs

Pattern docs capture reusable implementation patterns to reduce repeated ADRs.

- Keep each pattern short.
- Include a minimal example and constraints.

Recommended starter pattern docs:
- `docs/patterns/PAT_PUBLIC_API.md` (public API standards: versioning/errors/deprecation)
